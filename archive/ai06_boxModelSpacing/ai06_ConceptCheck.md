# ai06 Concept Check

Answer in your own words. (Competencies: 6.5.8; box model + positioning also feed the December Certiport exam directly.)

## 1. Name the four rings of the box model from the inside out. Which ring does a background color fill?

## 2. "Add more space inside the cards" and "add more space between the cards" are different CSS requests. Which property is which?

## 3. An element has width 200px, padding 20px on each side, and a 5px border. How wide is it on screen (default box-sizing)? Walk the arithmetic.

## 4. What is the difference between `display: none` and `visibility: hidden` — visually AND for a screen reader?

## 5. Describe the "sr-only" (screen-reader-only) technique: what does it look like on screen, what does a screen reader do with it, and why do professional sites use it?

## 6. What does `position: sticky` do, and why does a sticky header need a solid background?
